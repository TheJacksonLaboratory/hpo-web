import hpo.api.model.MaxoSearchResult

model {
  MaxoSearchResult maxoSearchResult
}

json{
  term tmpl.maxoTerm(maxoSearchResult.maxoTerm)
  synonymMatched maxoSearchResult.synonymMatch
  synonym maxoSearchResult.matchedSynonym
}
